namespace Bloxstrap
{
    /// <summary>
    /// Options chosen across the installer pages, shared with the installation logic.
    /// </summary>
    public class InstallOptions
    {
        public bool InstallGameManager { get; set; } = true;
    }
}
