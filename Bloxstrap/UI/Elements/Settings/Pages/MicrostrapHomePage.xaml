<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.MicrostrapHomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:models="clr-namespace:Bloxstrap.UI.ViewModels"
      xmlns:resources="clr-namespace:Bloxstrap.Resources"
      xmlns:app="clr-namespace:Bloxstrap"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="800"
      Title="Home"
      Scrollable="True">
    <StackPanel Margin="0,0,14,14">
        <Border Padding="24" CornerRadius="18" Background="#25213D" BorderBrush="#5D4C8B" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel MaxWidth="560">
                    <TextBlock Text="Welcome to Microstrap" FontSize="28" FontWeight="SemiBold" />
                    <TextBlock Margin="0,8,0,0" Text="A lightweight Roblox bootstrapper with useful tools, clean settings, and a faster way to get into your games." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                    <StackPanel Orientation="Horizontal" Margin="0,18,0,0">
                        <ui:Button Content="Play Roblox" Icon="Play24" Appearance="Primary" Command="models:GlobalViewModel.OpenWebpageCommand" CommandParameter="roblox://" />
                        <ui:Button Margin="8,0,0,0" Content="Open Roblox website" Icon="Open24" Command="models:GlobalViewModel.OpenWebpageCommand" CommandParameter="https://www.roblox.com/home" />
                        <!-- TEMPORARY: antivirus false-positive workaround, remove once Microstrap is code-signed -->
                        <ui:Button Margin="8,0,0,0" Content="Add to exclusions" Icon="Shield24" Click="AddToExclusions_Click" />
                    </StackPanel>
                </StackPanel>
                <Image Grid.Column="1" Width="104" Height="104" Margin="28,0,8,0" Source="pack://application:,,,/Microstrap.ico" RenderOptions.BitmapScalingMode="HighQuality" />
            </Grid>
        </Border>

        <Border Margin="0,12,0,0" Padding="14" CornerRadius="10" Background="#3A2020" BorderBrush="#A85C5C" BorderThickness="1">
            <TextBlock FontSize="13" TextWrapping="Wrap" Foreground="#FFE2E2"
                       Text="Make sure your Microstrap was downloaded from https://microstraplabs.freebuff.app/ or the official GitHub Repo. If you downloaded this binary from someone--or somewhere else, delete it and run a virus scan." />
        </Border>

        <TextBlock Margin="0,22,0,8" Text="Quick access" FontSize="20" FontWeight="SemiBold" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:CardAction Grid.Column="0" Margin="0,0,5,0" Icon="Games24" Command="models:GlobalViewModel.OpenWebpageCommand" CommandParameter="https://www.roblox.com/discover">
                <StackPanel><TextBlock Text="Discover games" FontSize="14" FontWeight="SemiBold" /><TextBlock Margin="0,3,0,0" Text="Find something new to play." FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" /></StackPanel>
            </ui:CardAction>
            <ui:CardAction Grid.Column="1" Margin="5,0,5,0" Icon="Settings24" Command="models:GlobalViewModel.OpenWebpageCommand" CommandParameter="https://microstraplabs.freebuff.app/">
                <StackPanel><TextBlock Text="Microstrap project" FontSize="14" FontWeight="SemiBold" /><TextBlock Margin="0,3,0,0" Text="View updates and source code." FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" /></StackPanel>
            </ui:CardAction>
            <ui:CardAction Grid.Column="2" Margin="5,0,0,0" Icon="QuestionCircle24" Command="models:GlobalViewModel.OpenWebpageCommand" CommandParameter="https://github.com/microstraplabs/microstrap/issues">
                <StackPanel><TextBlock Text="Get help" FontSize="14" FontWeight="SemiBold" /><TextBlock Margin="0,3,0,0" Text="Report a problem or request a feature." FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" /></StackPanel>
            </ui:CardAction>
        </Grid>

        <TextBlock Margin="0,22,0,8" Text="At a glance" FontSize="20" FontWeight="SemiBold" />
        <Border Padding="16" CornerRadius="14" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0"><TextBlock Text="STATUS" FontSize="10" Foreground="{DynamicResource TextFillColorTertiaryBrush}" /><TextBlock Margin="0,5,0,0" Text="Ready" FontSize="16" FontWeight="SemiBold" Foreground="#73E2A3" /><TextBlock Margin="0,3,0,0" Text="Microstrap is ready for Roblox" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" /></StackPanel>
                <StackPanel Grid.Column="1"><TextBlock Text="VERSION" FontSize="10" Foreground="{DynamicResource TextFillColorTertiaryBrush}" /><TextBlock Margin="0,5,0,0" Text="{x:Static app:App.Version}" FontSize="16" FontWeight="SemiBold" Foreground="#29D3E6" /><TextBlock Margin="0,3,0,0" Text="First public release" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" /></StackPanel>
                <StackPanel Grid.Column="2"><TextBlock Text="TIP" FontSize="10" Foreground="{DynamicResource TextFillColorTertiaryBrush}" /><TextBlock Margin="0,5,0,0" Text="Use Games" FontSize="16" FontWeight="SemiBold" Foreground="#DFA7FF" /><TextBlock Margin="0,3,0,0" Text="to search official recommendations" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" /></StackPanel>
            </Grid>
        </Border>
    </StackPanel>
</ui:UiPage>
