<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.MicrostrapChangelogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="800"
      Title="Changelog"
      Scrollable="True">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="Release history for Microstrap." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        <Border Margin="0,16,0,12" Padding="18" CornerRadius="14" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Border Padding="9,4" CornerRadius="8" Background="#303047"><TextBlock Text="0.1.0" FontWeight="SemiBold" Foreground="#29D3E6" /></Border>
                    <TextBlock Margin="10,4,0,0" Text="Initial release" FontSize="17" FontWeight="SemiBold" />
                </StackPanel>
                <TextBlock Margin="0,16,0,0" Text="The changelog will be updated when Microstrap 0.1.0 is officially released." FontSize="14" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" />
                <TextBlock Margin="0,8,0,0" Text="Until then, this page intentionally remains stable so it does not document unreleased changes." FontSize="13" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
        <Border Padding="18" CornerRadius="14" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
            <StackPanel><TextBlock Text="Future releases" FontSize="17" FontWeight="SemiBold" /><TextBlock Margin="0,6,0,0" Text="After 0.1.0 ships, this tab will contain versioned improvements, fixes, and breaking changes." FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" /></StackPanel>
        </Border>
    </StackPanel>
</ui:UiPage>
