<ui:UiPage x:Class="Bloxstrap.UI.Elements.Installer.Pages.GameManagerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:Bloxstrap.UI.Elements.Installer.Pages"
      xmlns:controls="clr-namespace:Bloxstrap.UI.Elements.Controls"
      mc:Ignorable="d"
      d:DesignHeight="560" d:DesignWidth="800"
      Title="GameManagerPage"
      Scrollable="True"
      Loaded="UiPage_Loaded">

    <StackPanel Margin="0,0,14,14">
        <TextBlock FontSize="20" FontWeight="SemiBold" Text="Game Manager" TextWrapping="Wrap" />
        <TextBlock FontSize="14" Text="The Game Manager is a separate app for browsing and searching every Roblox game, with detailed info for each one." TextWrapping="Wrap" />

        <TextBlock FontSize="20" FontWeight="SemiBold" Margin="0,20,0,0" Text="Choose your setup" TextWrapping="Wrap" />

        <controls:OptionControl Margin="0,14,0,0" Header="No Game Manager launcher" Description="Microstrap only. You launch Roblox like you always have.">
            <RadioButton GroupName="GameManagerMode" Content="Nothing extra gets installed" IsChecked="{Binding NoGameManager, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl Margin="0,10,0,0" Header="Game Manager launcher" Description="Adds a separate Game Manager app to your Microstrap install folder. Browse, search, and jump into any Roblox game.">
            <RadioButton GroupName="GameManagerMode" Content="Install the Game Manager (recommended)" IsChecked="{Binding InstallGameManager, Mode=TwoWay}" />
        </controls:OptionControl>

        <Border Margin="0,20,0,0" Padding="16" CornerRadius="12" Background="{DynamicResource SolidBackgroundFillColorSecondaryBrush}">
            <TextBlock FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"
                       Text="The Game Manager is installed to %LocalAppData%\Microstrap\GameManager and uses Roblox's own game discovery, so every search shows the same results as the Roblox app." />
        </Border>
    </StackPanel>
</ui:UiPage>
