<ui:UiPage x:Class="Bloxstrap.UI.Elements.Installer.Pages.WelcomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:Bloxstrap.UI.Elements.Installer.Pages"
      xmlns:models="clr-namespace:Bloxstrap.UI.ViewModels"
      xmlns:dmodels="clr-namespace:Bloxstrap.UI.ViewModels.Installer"
      xmlns:resources="clr-namespace:Bloxstrap.Resources"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance dmodels:WelcomeViewModel, IsDesignTimeCreatable=True}"
      d:DesignHeight="560" d:DesignWidth="800"
      Title="Home"
      Scrollable="True"
      Loaded="UiPage_Loaded">

    <ui:UiPage.Resources>
        <LinearGradientBrush x:Key="HeroBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#211B3D" Offset="0" />
            <GradientStop Color="#3D1D43" Offset="0.55" />
            <GradientStop Color="#7D2B3D" Offset="1" />
        </LinearGradientBrush>
    </ui:UiPage.Resources>

    <StackPanel Margin="0,0,14,18">
        <Border CornerRadius="18" Padding="28" Background="{StaticResource HeroBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="92" Height="92" CornerRadius="24" Background="#22FFFFFF" BorderBrush="#38FFFFFF" BorderThickness="1" VerticalAlignment="Top">
                    <Image Width="66" Height="66" Source="pack://application:,,,/Microstrap.ico" RenderOptions.BitmapScalingMode="HighQuality" />
                </Border>

                <StackPanel Grid.Column="1" Margin="24,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="MICROSTRAP" FontSize="14" FontWeight="Bold" Foreground="#FFE9B0" />
                        <Border Margin="10,0,0,0" Padding="8,3" CornerRadius="8" Background="#25FFFFFF">
                            <TextBlock Text="v0.1.0" FontSize="11" FontWeight="SemiBold" Foreground="#FFF8E7" />
                        </Border>
                    </StackPanel>
                    <TextBlock Margin="0,10,0,0" Text="The best free, lightweight&#x0a;Roblox bootstrapper." FontSize="29" FontWeight="SemiBold" Foreground="White" TextWrapping="Wrap" />
                    <TextBlock Margin="0,12,0,0" Text="Launch faster, keep things clean, and make Roblox feel like yours — without the bloat." FontSize="14" Foreground="#D8D0E5" TextWrapping="Wrap" />
                </StackPanel>
            </Grid>
        </Border>

        <TextBlock Margin="4,24,0,0" Text="Everything you need. Nothing you don't." FontSize="20" FontWeight="SemiBold" />
        <TextBlock Margin="4,4,0,0" Text="Microstrap keeps your Roblox setup simple, quick, and under your control." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <Grid Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:CardAction Grid.Column="0" Margin="0,0,5,0" Icon="Settings24">
                <StackPanel>
                    <TextBlock FontSize="14" FontWeight="SemiBold" Text="Lightweight" />
                    <TextBlock Margin="0,4,0,0" FontSize="12" Text="A small footprint that gets out of your way." TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                </StackPanel>
            </ui:CardAction>
            <ui:CardAction Grid.Column="1" Margin="5,0" Icon="Settings24">
                <StackPanel>
                    <TextBlock FontSize="14" FontWeight="SemiBold" Text="Your Roblox, your way" />
                    <TextBlock Margin="0,4,0,0" FontSize="12" Text="Useful tweaks and thoughtful defaults, all in one place." TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                </StackPanel>
            </ui:CardAction>
            <ui:CardAction Grid.Column="2" Margin="5,0,0,0" Icon="Info20">
                <StackPanel>
                    <TextBlock FontSize="14" FontWeight="SemiBold" Text="Open &amp; transparent" />
                    <TextBlock Margin="0,4,0,0" FontSize="12" Text="Built in the open, with no paid features or nonsense." TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                </StackPanel>
            </ui:CardAction>
        </Grid>

        <Border Margin="0,18,0,0" Padding="16" CornerRadius="12" Background="{DynamicResource SolidBackgroundFillColorSecondaryBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Ready when you are" FontSize="14" FontWeight="SemiBold" />
                    <TextBlock Margin="0,3,0,0" Text="Set up Microstrap in less than a minute." FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </StackPanel>
                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="→" FontSize="24" Foreground="#E86A4A" />
            </Grid>
        </Border>

        <Grid Margin="0,18,0,0">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding VersionNotice, Mode=OneWay}" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Width="28" RenderOptions.BitmapScalingMode="HighQuality" Source="pack://application:,,,/Resources/MessageBox/Warning.png" />
            <TextBlock Grid.Column="1" FontSize="13" Margin="12,0" Text="{Binding VersionNotice, Mode=OneWay}" TextWrapping="Wrap" />
            <ui:Button Grid.Column="2" Icon="ArrowDownload16" Content="Download" Command="models:GlobalViewModel.OpenWebpageCommand" CommandParameter="https://github.com/microstrap/microstrap/releases/latest" />
        </Grid>

        <TextBlock Margin="4,20,0,0" FontSize="13" Text="Click Next to choose your install options." Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
    </StackPanel>
</ui:UiPage>
