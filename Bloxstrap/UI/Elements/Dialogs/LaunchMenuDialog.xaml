<base:WpfUiWindow x:Class="Bloxstrap.UI.Elements.Dialogs.LaunchMenuDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:Bloxstrap.UI.Elements.Dialogs"
        xmlns:base="clr-namespace:Bloxstrap.UI.Elements.Base"
        xmlns:models="clr-namespace:Bloxstrap.UI.ViewModels"
        xmlns:resources="clr-namespace:Bloxstrap.Resources"
        mc:Ignorable="d"
        Title="Microstrap"
        MinWidth="0"
        MinHeight="0"
        Width="660"
        SizeToContent="Height"
        ResizeMode="NoResize"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" ShowMinimize="False" ShowMaximize="False" Title="Microstrap" Icon="pack://application:,,,/Microstrap.ico" CanMaximize="False" KeyboardNavigation.TabNavigation="None" />

        <StackPanel Grid.Row="1" Margin="24,18,24,24">
            <Border CornerRadius="18" Padding="24" Background="#241E3C">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Width="76" Height="76" CornerRadius="21" Background="#20FFFFFF" BorderBrush="#35FFFFFF" BorderThickness="1">
                        <Image Width="55" Height="55" Source="pack://application:,,,/Microstrap.ico" RenderOptions.BitmapScalingMode="HighQuality" />
                    </Border>
                    <StackPanel Grid.Column="1" Margin="18,0,0,0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="MICROSTRAP" FontSize="13" FontWeight="Bold" Foreground="#FFE9B0" />
                            <Border Margin="10,0,0,0" Padding="8,3" CornerRadius="8" Background="#25FFFFFF">
                                <TextBlock Text="{Binding Version, Mode=OneTime}" FontSize="11" FontWeight="SemiBold" Foreground="#FFF8E7" />
                            </Border>
                        </StackPanel>
                        <TextBlock Margin="0,6,0,0" Text="Roblox, without the extra weight." FontSize="22" FontWeight="SemiBold" Foreground="White" />
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Ellipse Width="8" Height="8" Fill="#73E2A3" VerticalAlignment="Center" />
                            <TextBlock Margin="7,0,0,0" Text="{Binding SetupStatus}" FontSize="12" Foreground="#D8D0E5" />
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>

            <TextBlock Margin="4,22,0,0" Text="Quick launch" FontSize="19" FontWeight="SemiBold" />
            <TextBlock Margin="4,3,0,10" Text="Choose where you want to go." FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:CardAction Grid.Column="0" Margin="0,0,5,0" Icon="ArrowRight12" TabIndex="0" Command="{Binding LaunchRobloxCommand, Mode=OneTime}">
                    <StackPanel>
                        <TextBlock FontSize="14" FontWeight="SemiBold" Text="{x:Static resources:Strings.LaunchMenu_LaunchRoblox}" />
                        <TextBlock Margin="0,3,0,0" Text="{Binding PlayerStatus}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    </StackPanel>
                </ui:CardAction>
                <ui:CardAction Grid.Column="1" Margin="5,0,0,0" Icon="Wrench24" Command="{Binding LaunchRobloxStudioCommand, Mode=OneTime}">
                    <StackPanel>
                        <TextBlock FontSize="14" FontWeight="SemiBold" Text="{x:Static resources:Strings.LaunchMenu_LaunchRobloxStudio}" />
                        <TextBlock Margin="0,3,0,0" Text="{Binding StudioStatus}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    </StackPanel>
                </ui:CardAction>
            </Grid>

            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:CardAction Grid.Column="0" Margin="0,0,5,0" TabIndex="1" Icon="Games28" Command="{Binding LaunchGameManagerCommand, Mode=OneTime}">
                    <StackPanel>
                        <TextBlock FontSize="14" FontWeight="SemiBold" Text="Game Manager" />
                        <TextBlock Margin="0,3,0,0" FontSize="12" Text="Browse and search every Roblox game." Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    </StackPanel>
                </ui:CardAction>
                <ui:CardAction Grid.Column="1" Margin="5,0,0,0" TabIndex="2" Icon="Settings28" Command="{Binding LaunchSettingsCommand, Mode=OneTime}">
                    <StackPanel>
                        <TextBlock FontSize="14" FontWeight="SemiBold" Text="{x:Static resources:Strings.LaunchMenu_ConfigureSettings}" />
                        <TextBlock Margin="0,3,0,0" FontSize="12" Text="Change performance, appearance, and integrations." Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    </StackPanel>
                </ui:CardAction>
            </Grid>

            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:CardAction Grid.Column="0" TabIndex="3" Icon="QuestionCircle48" Command="{Binding LaunchAboutCommand, Mode=OneTime}">
                    <StackPanel>
                        <TextBlock FontSize="14" FontWeight="SemiBold" Text="About Microstrap" />
                        <TextBlock Margin="0,3,0,0" FontSize="12" Text="Learn more about this v0.1.0 release." Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    </StackPanel>
                </ui:CardAction>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,18,0,0">
                <ui:Hyperlink Icon="Globe24" Content="Official site" NavigateUri="https://microstraplabs.freebuff.app/" Margin="0,0,20,0" />
                <ui:Hyperlink Icon="Code24" Content="GitHub" NavigateUri="https://github.com/microstraplabs/microstrap" />
            </StackPanel>
        </StackPanel>
    </Grid>
</base:WpfUiWindow>
